import 'package:flutter/material.dart';

class BakeryLoginScreen extends StatelessWidget {
  const BakeryLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bakery Login Screen"),
      ),
    );
  }
}